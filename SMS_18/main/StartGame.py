from datetime import datetime
from .models import GameSwitch, CurrentTime, StockPriceVariation, Stock

def start():
	#current_time = datetime.now()
	#for this_stock in Stock:
	#	objects = StockPriceVariation.objects.filter(stock_id = this_stock)
	#	for this_object in objects:
			print("I will finish this later")